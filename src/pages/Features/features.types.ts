export interface FeatureCardData {
  id: string;
  title: string;
  description: string;
  image: string;
  bgColor: string;
  borderColor: string;
  initialX: number; // %
  initialY: number; // %
}

